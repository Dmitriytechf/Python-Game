# Python Game

Проект с простыми играми на чистом python и с его библиотеками. Целью данного проекта является показать, что делать игры на python возможно и самое главное — интересно. Также можно создать .exe файл, чтобы перекинуть игру другу!


## 🛠 Установка и запуск
1. **Клонирование и настройка окружения:**
```bash
git clone <ссылка-на-репозиторий>
cd <имя-репозитория>

python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```
2. **Переход в папку с нужной игрой**
```bash
cd <имя-репозитория>
```
3. **Запуск**
```bash
python <имя-файла>.py

# Пример для змейки
python snake.py
```
**Создание .exe файла**
```bash
pyinstaller --onefile <путь/до/файла>

# Пример из папки с игрой:
pyinstaller --onefile snake.py
```
